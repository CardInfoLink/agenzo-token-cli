实现 ride 写命令：填充 src/ride/book.ts、src/ride/cancel.ts（foundation 已建骨架与 builder 契约，保持其导出签名）。两者均为写操作，必须 --idempotency-key，通过 Idempotency-Key 请求头发送（不放 body）；缺省时用 prompt-engine 索取，--yes 模式缺失则抛 PARAM_IDEMPOTENCY_KEY_REQUIRED（用 utils/idempotency）。仅依赖 core/types/utils，命令文件之间不互相 import。book.ts：POST /api/v2/rides/book；body 字段 quote_id、vehicle_class、price_amount、price_currency(默认 USD)、payment_method_id、passenger_name、passenger_phone，可选 passenger_email/luggage_count/special_requests、pickup{lat,lng,name}、dropoff{lat,lng,name}、pickup_time，可选 meet_and_greet(bool)/meet_and_greet_price/welcome_sign/arrival_flight{flight_no,airline}/departure_flight{...}；对应 flags --quote-id/--vehicle-class/--price-amount/--price-currency/--payment-method-id/--passenger-name/--passenger-phone/... ；金额为 decimal 货币单位(非分)；响应展示 ride_id(后续 get/cancel 用)、order_id、price 及 elife 透传字段；在 book.ts 顶部加注释 TODO：未来后端改造完成后将 --payment-method-id 切换为 --payment-order-id（支付订单反查模式，对应内部工单 BACK-034）。cancel.ts：POST /api/v2/rides/{order_id}/cancel，flag --order-id(=ride_id)，响应展示 ride_id、ride_stat、cancellation{cancellation_fee,reversal_amount,currency}。默认 --format json 输出 data，--format table 人类可读；支持 --help --format json 输出 schema；写操作非 --yes 时先确认；错误以后端 error.code 透传（如 BOOKING_FAILED、CANCELLATION_NOT_ALLOWED）。
