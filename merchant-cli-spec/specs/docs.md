编写 merchant-cli/README.md 与 merchant-cli/SKILL.md（依赖 foundation 完成后命令已定型；只写文档，不改任何代码文件）。README.md：包名 agenzo-merchant-cli、安装、bin 用法、命令清单（config set-host/reset-host/show；services list/get；ride quote/book/get/cancel/list-orders）、默认 JSON 与 --format table、--api-key/X-API-Key 认证、--idempotency-key 幂等、--yes、默认 host https://agent.everonet.com 与 config set-host；文中需出现字符串 agenzo-merchant-cli。SKILL.md：给 AI Agent 读的技能说明——能力发现(services list/get)、ride 闭环工作流 quote→book→get(轮询)→[cancel]、各命令参数要点、错误码与恢复建议（QUOTE_EXPIRED/VEHICLE_UNAVAILABLE/BOOKING_FAILED/CANCELLATION_NOT_ALLOWED/SERVICE_NOT_FOUND/PARAM_IDEMPOTENCY_KEY_REQUIRED）、写操作需幂等键、金额为 decimal 货币单位。
