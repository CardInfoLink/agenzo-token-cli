实现 services 能力发现（无需后端）：新建 src/services/registry.ts，并填充 foundation 已建骨架的 src/services/list.ts、src/services/get.ts（保持其 builder 导出签名）。仅依赖 core/types/utils，命令文件之间不互相 import。registry.ts：内置静态能力注册表，本期仅一条 ride：service_id、name、description、version、provider=elife、cli_noun=ride、verbs=[quote,book,get,cancel,list-orders]、since、discovery.help_command，并含 billing.mode=pay_per_call，以及每个 verb 的描述与 workflow 步骤 quote→book→get(poll)→[cancel] 供 get 使用。list.ts：services list 输出静态能力清单（本期只有 ride 一条），并回显 billing.mode=pay_per_call。get.ts：services get <service-id> 输出该能力详细元数据（verb 描述 + workflow 步骤）；未知 service-id 抛错误码 SERVICE_NOT_FOUND。默认 --format json，--format table 人类可读；支持 --help --format json 输出 schema。
